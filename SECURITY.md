# 보안 안내

## 비밀번호 저장

- Windows: Data Protection API(DPAPI)의 `CurrentUser` 범위로 암호화하며
  `%LOCALAPPDATA%\KakaoQuickLogin\password.dat`에 저장합니다. 파일만 다른 PC나 다른
  Windows 사용자에게 복사해도 복호화할 수 없습니다.
- macOS: 데이터 보호 키체인의 일반 암호 항목으로 저장하며, 접근성은
  `WhenUnlockedThisDeviceOnly`로 제한합니다. 다른 Mac으로 동기화되지 않습니다.

DPAPI와 키체인은 같은 사용자 권한으로 실행되며 필요한 권한을 가진 악성 프로그램까지 막는
완전한 격리 장치는 아닙니다. 운영체제를 최신 상태로 유지하고 신뢰할 수 없는 프로그램과
함께 사용하지 마세요.

## 데이터 흐름

- 애플리케이션 자체에는 네트워크 전송 코드가 없습니다.
- 평문 비밀번호를 디스크나 클립보드에 기록하지 않습니다.
- Windows는 로그인 직전에 복호화한 바이트·문자 버퍼를 사용 후 지웁니다.
- macOS는 키체인에서 읽은 데이터와 키보드 입력용 UTF-16 버퍼를 사용 후 지웁니다. Swift
  `String` 등 런타임이 만드는 모든 임시 복사본의 즉시 제거까지 보장하지는 않습니다.
- 로그 파일이나 분석 정보를 수집하지 않습니다.

## 자동 입력 보호

입력 전에 다음 항목을 모두 검사합니다.

1. 설치된 카카오톡 앱과 실행 중인 프로세스 경로가 일치하는지
2. 실행 앱의 번들 또는 실행 파일이 카카오톡인지
3. 카카오톡이 전면 앱인지
4. 입력란이 비밀번호 필드이고 실제 포커스된 요소인지
5. 활성 로그인 버튼을 접근성 정보로 확인했는지

검사가 하나라도 실패하면 비밀번호 입력을 중단합니다.

macOS에서 접근성 값 설정이 지원되지 않는 보안 입력란은 대상 카카오톡 프로세스에만
키보드 이벤트를 전달합니다. 이 경우에도 전면 앱과 포커스된 보안 입력란을 다시 확인하며
클립보드는 사용하지 않습니다.

## 취약점 제보

공개 이슈에 비밀번호, 계정, 암호화 파일 또는 개인 정보를 올리지 마세요. 배포 전에
저장소 관리자가 비공개 보안 제보 채널을 별도로 정해야 합니다.
